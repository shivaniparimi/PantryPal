GEMINI_API_KEY=your_api_key_here

# Optional — only needed for Google sign-in verification and saved recipes (Firestore).
# The full contents of a Firebase service account JSON key, as a single-line string.
FIREBASE_SERVICE_ACCOUNT_KEY=

# Optional — restricts CORS to a specific frontend origin in production. Defaults to allow all.
ALLOWED_ORIGIN=
